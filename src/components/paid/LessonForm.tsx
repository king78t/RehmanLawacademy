import { Save, X } from "lucide-react";
import { useEffect, useState, type FormEvent } from "react";
import type { PaidLesson, PaidModule } from "@/lib/paid-course-types";

const empty = { module_id: "", title: "", description: "", notes: "", attachment_url: "", video_provider: "", video_reference: "", duration_seconds: 0, sort_order: 1, is_published: false };
export function LessonForm({ courseId, courseSlug, modules, editing, onSaved, onCancel }: { courseId: string; courseSlug: string; modules: PaidModule[]; editing?: PaidLesson | null; onSaved: (record: any) => Promise<void>; onCancel: () => void }) {
  const [form, setForm] = useState<any>(empty); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  useEffect(() => { setForm({ ...empty, module_id: modules[0]?.id || "", ...editing, is_published: Boolean(editing?.is_published) }); setError(""); }, [editing, modules]);
  const update = (key: string, value: any) => setForm((current: any) => ({ ...current, [key]: value }));
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!form.module_id || !modules.some((item) => item.id === form.module_id)) { setError("Choose the module that owns this lesson."); return; } if (!String(form.title).trim()) { setError("Add a lesson title."); return; } const order = Number(form.sort_order); const duration = Number(form.duration_seconds); if (!Number.isInteger(order) || order < 1 || !Number.isFinite(duration) || duration < 0) { setError("Order and duration must be zero or greater, with order starting at one."); return; } setSaving(true); setError(""); try { await onSaved({ course_id: courseId, course_slug: courseSlug, module_id: form.module_id, title: String(form.title).trim(), description: String(form.description || "").trim(), notes: String(form.notes || "").trim(), attachment_url: String(form.attachment_url || "").trim(), video_provider: String(form.video_provider || "").trim(), video_reference: String(form.video_reference || "").trim(), duration_seconds: duration, sort_order: order, is_published: Boolean(form.is_published) }); } catch (saveError) { console.error("Failed to save lesson:", saveError); setError("The lesson could not be saved. Try again."); } finally { setSaving(false); } };
  return <form onSubmit={submit} className="surface p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="eyebrow">{editing ? "Edit lesson" : "New lesson"}</p><h3 className="mt-2 font-display text-xl font-bold text-[#14294d]">Protected lesson details</h3></div><button type="button" className="button-quiet !min-h-10 !min-w-10 !p-2" onClick={onCancel} aria-label="Close lesson form"><X size={18} /></button></div><p className="mt-3 rounded-xl border border-[#cfe1ef] bg-[#f1f8fc] p-3 text-xs leading-5 text-[#4f6982]">Video references stay out of the public catalog. Add a provider and reference only when the protected lesson is ready.</p>{error && <p className="mt-4 rounded-xl bg-rose-50 p-3 text-xs font-semibold leading-5 text-rose-700" role="alert">{error}</p>}<div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600" htmlFor="lesson-module">Module<select id="lesson-module" value={form.module_id} onChange={(event) => update("module_id", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-sky-400"><option value="">Choose module</option>{modules.map((module) => <option key={module.id} value={module.id}>{module.title}</option>)}</select></label><label className="text-xs font-bold text-slate-600" htmlFor="lesson-title">Title<input id="lesson-title" value={form.title} onChange={(event) => update("title", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label></div><label className="mt-4 block text-xs font-bold text-slate-600" htmlFor="lesson-description">Description<textarea id="lesson-description" rows={2} value={form.description} onChange={(event) => update("description", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-sky-400" /></label><label className="mt-4 block text-xs font-bold text-slate-600" htmlFor="lesson-notes">Study notes<textarea id="lesson-notes" rows={4} value={form.notes} onChange={(event) => update("notes", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-sky-400" /></label><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600" htmlFor="lesson-provider">Video provider<input id="lesson-provider" value={form.video_provider} onChange={(event) => update("video_provider", event.target.value)} placeholder="direct, youtube, private-provider" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label><label className="text-xs font-bold text-slate-600" htmlFor="lesson-reference">Private video reference<input id="lesson-reference" value={form.video_reference} onChange={(event) => update("video_reference", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label><label className="text-xs font-bold text-slate-600" htmlFor="lesson-duration">Duration, seconds<input id="lesson-duration" type="number" min="0" value={form.duration_seconds} onChange={(event) => update("duration_seconds", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label><label className="text-xs font-bold text-slate-600" htmlFor="lesson-order">Display order<input id="lesson-order" type="number" min="1" step="1" value={form.sort_order} onChange={(event) => update("sort_order", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label></div><label className="mt-4 block text-xs font-bold text-slate-600" htmlFor="lesson-attachment">Protected attachment URL, optional<input id="lesson-attachment" value={form.attachment_url} onChange={(event) => update("attachment_url", event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-sky-400" /></label><label className="mt-4 flex min-h-11 items-center gap-3 rounded-xl border border-slate-200 p-3 text-xs font-bold text-slate-600"><input type="checkbox" checked={Boolean(form.is_published)} onChange={(event) => update("is_published", event.target.checked)} className="h-4 w-4 accent-[#1766a9]" /> Publish for active students</label><div className="mt-5 flex gap-3"><button type="submit" disabled={saving || !modules.length} className="button-primary"><Save size={15} /> {saving ? "Saving…" : editing ? "Save lesson" : "Add lesson"}</button><button type="button" className="button-secondary" onClick={onCancel}>Cancel</button></div></form>;
}
