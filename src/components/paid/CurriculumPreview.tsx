import { CheckCircle2, ChevronRight, Layers3, LockKeyhole } from "lucide-react";
import type { PaidModule } from "@/lib/paid-course-types";

export function CurriculumPreview({ modules, active = false }: { modules: PaidModule[]; active?: boolean }) {
  if (!modules.length) return <div className="empty-panel py-10"><div className="mx-auto flex h-11 w-11 items-center justify-center rounded-2xl bg-slate-100 text-slate-400"><Layers3 size={19} /></div><h3 className="mt-4 font-display text-lg font-bold text-[#14294d]">Curriculum is being configured</h3><p className="mx-auto mt-2 max-w-md text-xs leading-5 text-slate-500">The academy will publish modules and lessons here as the course is prepared. No lesson access is implied until an administrator approves enrollment.</p></div>;
  return <div className="grid gap-3">{modules.map((module, index) => <article key={module.id} className="surface p-4 sm:p-5"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#eaf4fb] text-sm font-extrabold text-[#1766a9]">{String(index + 1).padStart(2, "0")}</div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="break-words font-display text-lg font-bold text-[#14294d]">{module.title}</h3>{module.description && <p className="mt-1 text-xs leading-5 text-slate-500">{module.description}</p>}</div><span className="inline-flex shrink-0 items-center gap-1.5 rounded-full bg-[#f7fafc] px-2.5 py-1 text-[.62rem] font-bold text-slate-500">{active ? <CheckCircle2 size={13} className="text-[#2e7655]" /> : <LockKeyhole size={13} />} {active ? "Available" : "Preview"}</span></div><div className="mt-4 flex items-center gap-2 border-t border-slate-100 pt-3 text-xs font-bold text-slate-400"><ChevronRight size={14} className="text-[#b28d37]" /> Lesson details appear after enrollment approval</div></div></div></article>)}</div>;
}
